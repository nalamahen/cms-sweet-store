<%- include('../_layouts/adminheader') %>

<h2 class="page-title">Edit product</h2>
<a href="/admin/products" class="btn btn-primary">Back to all products</a>
<br><br>

<form method="post" action="/admin/products/edit-product/<%= id %>" enctype="multipart/form-data">
    
    <div class="form-group">
        <label for="">Name</label>
        <input type="text" class="form-control" name="name" value="<%= name %>" placeholder="Name">
    </div>
    
    <div class="form-group">
        <label for="">Description</label>
        <textarea name="desc" class="form-control" cols="30" rows="10" placeholder="Description"><%= desc %></textarea>
    </div>
    
    <div class="form-group">
        <label for="">Brand</label>
        <select name="brand" class="form-control">
            <% brands.forEach(function(brnd){ %>
                <option value="<%= brnd.slug %>"
                <% if (brnd.slug == brand) { %>
                selected="selected"
                <% } %>        
                ><%= brnd.name %></option>
            <% }); %>
        </select>
    </div>

    <div class="form-group">
            <label for="">Category</label>
            <select name="category" class="form-control">
                <option value="">select category</option>
                <% categories.forEach(function(cate){ %>
                <option value="<%= cate.slug %>"
                    <% if (cate.slug == category) { %>
                    selected="selected"
                    <% } %>        
                ><%= cate.name %></option>
            <% }); %>
            </select>
        </div>
    
    <div class="form-group">
        <label for="">Price</label>
        <input type="text" class="form-control" name="price" value="<%= price %>" placeholder="Price">
    </div>
    
    <div class="form-group">
        <label for="">Current Image</label>
        <p>
            <% if (image == "") { %>
            <img id="noimage" src="/images/noimage.png" alt="">
            <% } else {%>
            <img id="noimage" src="<%= productImageUrl %>/<%= image %>" alt="">
            <% } %>
        </p>
    </div>
    
    <div class="form-group">
        <label for="">Upload Image</label>
        <input type="file" class="form-control" name="image" id="img">
        <img src="#" id="imgPreview" alt="">
    </div>
    
    <div class="form-group">
        <label for="">In Stock</label>       
        <input type="checkbox" name="instock" <%if(instock){%> checked <%}%> >
    </div>

    <div class="form-group">
        <label for="">Vat Applicable</label>       
        <input type="checkbox" name="vat" <%if(vat){%> checked <%}%> >
    </div>

    <div class="form-group">
        <label for="">Featured</label>       
        <input type="checkbox" name="featured" <%if(featured){%> checked <%}%> >
    </div>

    <div class="form-group">
        <label for="">Product Code</label>       
        <input type="text" name="product_code" value=" <%= product_code %>">
    </div>

    <input type="hidden" name="pimage" value="<%= image %>">
    <button class="btn btn-primary">Submit</button>
</form>

<%- include('../_layouts/adminfooter') %>